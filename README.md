# BM USA Proxy — visual demo

Clickable UI prototype for **BM USA Proxy** in the brand palette — a Telegram mini-app (customers) + a web admin panel (operator). Visual only (no backend), realistic mock data.

**Live:** open `index.html` and pick a surface.

- `index.html` — start here (launcher)
- `admin.html` — operator admin panel · 11 screens (click the sidebar)
- `miniapp.html` — Telegram mini-app · 6 screens (tap the bottom nav)

Self-contained static HTML — no build step. Hosted via GitHub Pages.
